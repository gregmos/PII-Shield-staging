--extra-index-url https://download.pytorch.org/whl/cpu

# Core ML stack (CPU-only torch keeps install ~800 MB instead of 4 GB)
torch==2.3.0+cpu
transformers==4.44.2
gliner==0.2.13
onnxruntime==1.18.1

# Presidio PII detection
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355

# spaCy + small English model (used by presidio + as NER fallback)
spacy==3.7.5
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1.tar.gz

# Document I/O
python-docx==1.1.2
pdfminer.six==20240706

# MCP runtime (subprocess mode does not need it, but pii_shield_server.py imports it at module load)
mcp[cli]==1.2.0
cryptography==42.0.5
