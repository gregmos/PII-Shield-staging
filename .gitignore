# IDE / editor
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Claude Code workspace metadata
.claude/

# Test / tool scratch output
*.output
*.bak
*.tmp

# Root-level dist/ is legacy v1.x output — never committed.
# Product build artifacts live under nodejs-v2/dist/ and that subtree has
# its own .gitignore.
dist/

# Out-of-scope sibling projects / scratch dirs that occasionally appear
# under this worktree during development
apps-tester/
host-bridge-probe/
review-flow-tester/
scratch/
pii-shield-diag/
pii-shield-diag.zip
pii-shield-plugin-test/
pii-shield-plugin-test.zip
MCP.mcpb
manifest.json.bak
